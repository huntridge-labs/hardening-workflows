# Hardening Dagger Module - Python Dependencies
#
# These versions are pinned for reproducibility.
# Use Dependabot or Renovate to automate version updates with PR checks.
#
# To update: pip-compile requirements.in -o requirements.txt
# Or manually update versions and run tests.

# Dagger SDK - Core dependency for the module
# https://pypi.org/project/dagger-io/
dagger-io==0.19.9

# Note: The following are transitive dependencies of dagger-io.
# They are pinned here for reproducibility and security scanning.
# dagger-io will install these automatically, but pinning ensures consistency.

anyio==4.8.0
attrs==25.1.0
beartype==0.19.0
cattrs==24.1.2
gql==3.5.0
graphql-core==3.2.5
httpx==0.28.1
certifi==2025.1.31
httpcore==1.0.7
idna==3.10
sniffio==1.3.1
h11==0.14.0
platformdirs==4.3.6
rich==13.9.4
markdown-it-py==3.0.0
mdurl==0.1.2
pygments==2.19.1
typing_extensions==4.12.2
