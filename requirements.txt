# Test requirements file for Safety vulnerability scanning
# These packages have known vulnerabilities for testing purposes
# WARNING: DO NOT USE IN PRODUCTION!

# Vulnerable package with known CVEs for testing
django==4.2.26  # Has multiple security vulnerabilities
requests==2.32.4  # Older version with known issues
urllib3==2.5.0  # Contains security vulnerabilities
Pillow==10.3.0  # Has image processing vulnerabilities
flask==2.2.5  # Older version with security issues
jinja2==3.1.6  # Template injection vulnerabilities
sqlalchemy==1.0.0  # SQL injection possibilities
